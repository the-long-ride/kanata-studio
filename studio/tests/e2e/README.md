# Desktop E2E
Native keyboard interception tests require OS runners/hardware permissions. The release matrix in `docs/STUDIO_TEST_MATRIX.md` is mandatory; frontend interaction tests run in Vitest, while native packaging and Rust service tests run in GitHub Actions.
